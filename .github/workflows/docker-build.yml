name: Docker Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Docker Build
      run: |
        echo ${GITHUB_REF#refs/*/}
        git describe --tags || true
        docker build -t cfarrend/flower:$(git describe --tags) .

